<div class="container">
  <h2>注文履歴詳細</h2>
  <table>
    <thead>
      <tr>
      <%= form_with model: @order, url: admin_order_path do |o| %>
        <th><b>購入者</b></th>
        <td><%= o.name %></td>
        </tr>
        <tr>
          <th><b>注文日</b></th>
          <td><%= o.created_at.strftime("%Y/%m/%d") %></td>
        </tr>
        <tr>
          <th><b>配送先</b></th>
          <td>
            〒<%= o.postcode %><br>
            <%= o.address %><br>
            <%= o.neme %>
          </td>
        </tr>
        <tr>
          <th><b>支払方法</b></th>
          <td><%= o.payment_method %></td>
        </tr>
        <tr>
          <th><b>注文ステータス</b></th>
          <td>
            <%= form_with model: @order, url: admin_order_path, method: :patch do |o| %>
            <%= o.select :order_status, Order.order_statuses.keys, class: "order_status" %>
            <%= o.submit "更新" , class: "btn btn-outline-success" %>
          </td>
        </tr>
    </thead>
  </table>
</div>